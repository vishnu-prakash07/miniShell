# miniShell
A mini Unix-like shell implemented in C that supports command execution, argument parsing, and basic built-in commands. Built to understand process creation, system calls, and operating system internals.
